"use client"; // এটি Client Component হিসেবে চিহ্নিত করবে
import React from "react";
// import { useDispatch } from "react-redux";
// import { addEmployee } from "../redux/slice";

const BioData = () => {
  const huku = {
    name: "joy bunga",
  };

  // const dispatch = useDispatch();

  // dispatch এর মাধ্যমে সঠিকভাবে একশন পাঠানো
  // dispatch(addEmployee(huku));

  return <div></div>;
};

export default BioData;
